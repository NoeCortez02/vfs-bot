services:
  - type: web
    name: vfs-bot
    env: node
    plan: free
    buildCommand: npm install && apt-get update && apt-get install -y chromium-browser
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
